

<%= form_for([@user, @user.followers.build]) do |f| %>
  <p>
    <%= f.label :follower %><br />
    <%= f.text_field :follower %>
  </p>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
